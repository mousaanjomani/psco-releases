# PSCO Releases

Update packages for PSCO are published here as GitHub Releases.
Each release carries a `psco-update-<version>.zip` asset; the installed app checks `releases/latest` and updates itself.
